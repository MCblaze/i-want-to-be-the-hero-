# Main scene visual and gameplay repair — 16 September 2026

Baseline: a3b4ec5. This is an in-progress production pass, not release qualification.

## Reproduced in live Unity

- Ground art was below the collision surface, making Logan and enemies visibly float.
- Full-width terrain illustrations were squeezed onto short ledges, making their stone detail far smaller than the ground.
- Six background cards exposed rectangular edges; their fixed world positions and height did not cover the camera view.
- Twenty-one placeholder scenery renderers still drew translucent rectangles over production backgrounds.
- Main had no HeroTuning connection. Double jump, wand swapping/firing and backflip were conditional on the movement lab. Earlier statements that these were playable in Main overstated completion.

## Repairs

- Sixteen static surface skins now use sliced sprites with their walk-line pivots aligned to collider tops; ten ledges use a separate detail crop. Original source PNGs remain unchanged.
- The teal lift uses a walk-line sprite and aligns to its collider top.
- Placeholder backdrop/trunk/canopy/water renderers are disabled while their editable objects remain.
- Zone cards fill the camera view with overscan and subtle drift, maintain an opaque base, and crossfade in world-route order. The background is toned down for actor/terrain separation. Sibling references are cached.
- MainSceneLayout references the existing MovementTuning asset; controller abilities now read it in Main as well as the lab. Main's existing Spark sword damage is preserved.
- Five hazard placeholders now use a transparent thorn cluster with the existing hazard bounds and no added colliders.
- Main camera limits now account for the actual viewport width, preventing the old widescreen clamp from hiding Logan in narrow windows.
- Live Canopy review exposed large opaque particle squares. The water-mote system had no material and used size 1. It now uses a transparent soft-mote shader, 0.035–0.075-unit particles, a 24-particle cap and lifetime fade. The shader compiled without errors and live capture confirmed the squares disappeared. Reduced-effects mode clears/stops the particles.

## Validation

- Focused Main play-mode test passed after fixing test-runner frame synchronization: wand switch/fire, neutral backflip, air-jump availability, and opaque background coverage at 7 route positions across 3 aspect ratios.
- Final focused Main check: **1/1 passed**, including visibility at both route edges across square, 16:9 and 21:9 views. Platform regression: **2/2 passed** (idle animation on moving support; one-way landing, carry and low ceiling). Main authoring/lifecycle: **5/5 passed**, including twenty restarts and ten checkpoint-preserving falls. XML evidence is in `docs/evidence/polish-20260916/`. Air-jump availability is not a full double-jump animation/art acceptance test.
- The editor's AI Account API timeout remains an external service warning. The initial Windows build identified the missing-script warning on `Ambient Inhabitant - notice react settle`: presentation classes shared one source file and some scene references used embedded MonoScript objects. The three presentation components were split into matching source files, and Main references were migrated to real script assets without changing their behaviour or serialized settings. Final build verification is recorded below.
- `trailhead-verified.png` and `canopy-verified.png` are live camera captures after repairs, not concept images. Camera repositioning was used to inspect Canopy; this is not evidence of a complete unassisted route playthrough.

## Generated hazard provenance

`Assets/Art/Sunleaf/Terrain/ThornHazard.png` is original AI-generated artwork from this production pass. Direction: side-on Sunleaf mossy stone and brown-purple thorn cluster, ivory tips, pixel-art treatment, no perspective top plane, true transparent background. The first candidate had a baked checkerboard and was rejected. The second was a transparency edit, saved immediately to local staging and Unity; PNG color type 6 confirms RGBA. Native Sprite cropping aligns the visible hazard to its existing collision bounds. Source PNG is retained unchanged. Final visual/client acceptance remains open.

## Interface and progression follow-up

- Added Esc/controller Start pause, Resume and Restart Quest, plus a touch pause control. Pausing freezes simulation/audio, clears queued input, blocks attacks/swap, and restores time/audio on resume or restart. Standalone focus loss requests pause; physical device/focus-loss qualification remains open.
- HUD now reports the selected weapon and actual evade cooldown. Spark instructions explain the second jump and neutral backflip. Touch now includes weapon swap.
- UI frame uses nine-slice borders rather than stretching its ornamental corners across the whole card. Pause layout was visually inspected in Unity and adjusted to keep text and buttons inside the frame.
- Added an original transparent checkpoint lantern and Hero Seed crystal from `ProgressionProps.png`. Generated as side-on mossy-stone/bronze waymarker and gold crystal with three green leaves; both outer corner and inter-prop alpha were verified zero. Native crops keep the source unchanged. The unreachable duplicate Spark preview is hidden. Trigger positions/geometry are unchanged; checkpoint lantern faces away from the adjacent ledge.
- Reduced-effects mode is reapplied when a new quest starts, preventing restarted particles from ignoring the active preference.
- Final combined Main acceptance suite: **10/10 passed, 0 failed, 0 skipped**. This includes accessibility, death/checkpoint state, 20 restarts, camera coverage, pause input blocking, restart from pause and reduced-effects persistence. The XML is `docs/evidence/polish-20260916/acceptance-results.xml`. Physical controller, touch-device, focus-loss and full unassisted-route validation remain open.
- Final Windows x64 build after script-reference repair: **Succeeded, 0 errors, 0 warnings**, 176,535,446 bytes reported by Unity. Local executable: `unity/Builds/Polish-20260916/HeroDemo.exe` (build output is intentionally Git-ignored; source/assets/evidence are backed up). The redundant ambient component with identical settings was removed; Main retains 6 parallax anchors, 15 bounded-motion components and 1 ambient controller. Post-repair scene editability passed 1/1. Build report and scene-reference XML accompany the evidence.
- Direct interaction with the standalone executable could not begin: the Computer Use app-approval request timed out. No standalone start, input, focus-loss or full-route test is claimed. Editor tests and visual inspections above did run.

## Remaining release work

- Dedicated Logan wand/double-jump/backflip/defeat artwork and sword/crop repairs remain open.
- Target/collectible/landmark artwork, platform cap variety and environment animation require further production passes; thorn readability still needs fresh-player observation.
- Audio mix/listening approval, footsteps, victory music, display/settings and standalone focus-loss qualification remain open. Pause implementation is now present; device qualification is still needed.
- The approximately 51-unit route has no evidence supporting a 10–15 minute first completion; route expansion and timed fresh-player sessions remain necessary. Do not claim the timing target is met.
- Controller, standalone performance, five-player sessions and client acceptance remain unverified.

## Backup policy

Commit validated batches with Unity metadata, push them to MCblaze's GitHub repository, and preserve generated source files and reproducible asset tooling. No OneDrive storage.

## Standalone interface verification follow-up

- Standalone app access succeeded on retry. Directly verified title launch, Begin the quest, E weapon switching, Escape pause and Resume. The rebuilt game also pauses when focus moves to Unity and stays paused on return. This supersedes the earlier app-access blocker.
- Fixed the late objective overlapping weapon status: health, objective and weapon now occupy three distinct HUD rows. Boss health occupies the upper right. Gameplay HUD is hidden on title/victory screens.
- Expanded the title card and repositioned controls/options inside its frame. Moved victory text/replay inward. Inspected the rebuilt standalone at an approximately 854 x 480 client area; title and initial HUD fit. Long-objective and victory layouts were also inspected in the editor using deliberately arranged progression states, not route completion tests.
- Windows x64 rebuild succeeded: 0 errors, 0 warnings, 176,535,446 bytes. Evidence is in `docs/evidence/standalone-ui-20260916/`. The victory-after image is an arranged editor preview; its timer is not traversal evidence.
- Observed the earlier standalone reach victory at 3:26 while gameplay was progressing outside our direct movement control. Preserved `standalone-victory-3m26s.png`; this is an observed completion, not a controlled or first-time-player qualification. The 10–15 minute target remains unmet/unproven and route expansion remains required.
- Visible remaining defect: WAND status switches correctly, but Logan still displays the baked-in sword. Dedicated weapon animation art remains a production blocker. Controller/touch hardware, performance, full controlled route and fresh-player testing remain open.

## Player-reported blocked underpasses

Raised Main ledges 01, 02, 04, 06, 08 and 09 to y=-1.85, preserving their child art/collider alignment. Previously only 0.81–0.96 units remained above the floor, less than Logan's 1.15-unit capsule. Clearance is now 1.41 units (0.26 margin). Updated MainSceneBuilder to preserve the fix when authoring again. Unity horizontal CapsuleCastAll sweeps with Logan's actual 0.52 x 1.15 body passed under all six ledges; this is a geometry check, not a full route playthrough. Solid tops remain available for landing. New standalone output: unity/Builds/Underpass-20260916/HeroDemo.exe. An already-running older executable will not receive scene changes.

## Client delivery execution and first prop package

Created a detailed staged delivery plan in docs/CLIENT-DELIVERY-EXECUTION-20260916.md and the connected Unity records. Bezi created its private Sunleaf Client Delivery Page, inspected source references and authored corrected delivery specifications. Rejected its initial universal 32-PPU/large-arch assumption; sizing now derives from actual world geometry. Bezi has no generation action; Codex generated three original RGBA sources and saved them immediately in Assets/Art/Sunleaf/DeliveryGenerated.

Integrated a slender mossy rune barrier and visually distinct sword/wand target plaques. Gate collider stays 0.30 x 4.60; decorative silhouette is 0.68 x 4.60. Target collision remains 0.96 square; visible crops are approximately 0.96 x 0.93. All sources contain transparent pixels; native Sprite assets crop without altering originals. Gameplay-scale captures show matching forest palette and readable weapon emblems. Arranged inspection positions were used, not a complete route run. Both target hit counters advanced through the existing HeroAttack path. Gate and its child skin become inactive after CollectHeroSpark. Screenshots: docs/evidence/delivery-20260916.

Still open: final Logan animation pack, route expansion/timing, further world art/motion, final audio/settings, full regression and human/device/client qualification. This batch is not a client-ready release.
